import { LongitudDirective } from './longitud-directive';

describe('LongitudDirective', () => {
  it('should create an instance', () => {
    const directive = new LongitudDirective();
    expect(directive).toBeTruthy();
  });
});
