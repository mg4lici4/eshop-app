import { DigitosDirective } from './digitos-directive';

describe('DigitosDirective', () => {
  it('should create an instance', () => {
    const directive = new DigitosDirective();
    expect(directive).toBeTruthy();
  });
});
